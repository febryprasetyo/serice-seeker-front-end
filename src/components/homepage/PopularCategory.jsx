// import { stats } from '../../constants';
import styles from '../../style';
const PopularCategory = () => {
  return (
    <section
      className={`${styles.flexCenter} flex-row flex-wrap sm:mb-20 mb-6`}></section>
  );
};

export default PopularCategory;
